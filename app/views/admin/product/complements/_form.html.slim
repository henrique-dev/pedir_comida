= form_for @product_complement do |f|
  - if @product_complement.errors.any?
    #error_explanation
      h2 = "#{pluralize(@product_complement.errors.count, "error")} prohibited this product_complement from being saved:"
      ul
        - @product_complement.errors.full_messages.each do |message|
          li = message

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :price
    = f.text_field :price
  .actions = f.submit
